breaking changes log; changes from left hand to right demarked with >>>> :
COMMIT WHERE IT BROKE: 11d8184b38f5dde4daed2d1e710ba166bffe4113
    v1.11.7 animation values scale update function functionaly did nothing. tweak jitter val but comment out func. call.
COMMIT WHERE IT WORKED: 44401fae0a48f9e389a491bc6b6659daa07fb5bf
	oops animation scaling and orbit was off--maybe I'll want to reset to previous defaults
	v1.11.6

POSSIBLY BREAKING ADDITIONS in commit 11d8184b38f5dde4daed2d1e710ba166bffe4113 :
wanderedXY = new PVector(0,0);
original_max_jitter_dist = max_jitter_dist;
original_max_wander_dist = max_wander_dist;
original_motion_vector_max = motionVectorMax;
original_diameter_morph_rate = diameter_morph_rate;
jitter_max_step_mult = jitter_max_step_mult / animation_scale_multiplier;
motion_vector_max = original_motion_vector_max * animation_scale_multiplier;
		diameter_morph_rate = original_diameter_morph_rate * animation_scale_multiplier;
		max_jitter_dist = original_max_jitter_dist * animation_scale_multiplier;

POSSIBLY BREAKING CHANGES in commit 11d8184b38f5dde4daed2d1e710ba166bffe4113 :
float motion_vector_max; >>>> float motion_vector_max; float original_motion_vector_max;
jitter_max_step_mult = random(0.002, 0.0032);  // remember subtle values I like the results of: random(0.002, 0.058) -- for smaller circles/shapes. For YUGE: 0.002, 0.038 >>>> jitter_max_step_mult = random(0.007, 0.0097);  // 0.005 amounts to nothing visible.


change these back to TRUE before exe export! :

boolean tryToTweet = false;
boolean USE_FULLSCREEN = false;
boolean savePNGs = false;  // Save PNG images or not
boolean saveSVGs = false;  // Save SVG images or not

TRYING TO HANDLE the following with new museumMode boolean:
and change these back to:
  //String FNNE = get_detailed_image_file_name_no_ext();
  String FNNE = get_simple_file_name_no_ext();
  //LOCAL FOLDER SAVE:
  //saveFrame(FNNE + ".png");

  // CLOUD SAVE option one (SMOFA kiosk, Windows) ;
   saveFrame("C:\\Users\\SMOFA_guest\\Dropbox\\By_Small_and_Simple_Things__SMOFA_visitor_image_saves\\" + FNNE + ".png");

    //simpletweet = new SimpleTweet(this);
    //simpletweet.setOAuthConsumerKey(twitterAPIauthLines[0]);
    //simpletweet.setOAuthConsumerSecret(twitterAPIauthLines[1]);
    //simpletweet.setOAuthAccessToken(twitterAPIauthLines[2]);
    //simpletweet.setOAuthAccessTokenSecret(twitterAPIauthLines[3]);

    String svg_file_name_no_ext = get_detailed_image_file_name_no_ext();
    // String svg_file_name_no_ext = get_simple_file_name_no_ext();
    // beginRecord(SVG, "C:\\Users\\SMOFA_guest\\Dropbox\\By_Small_and_Simple_Things__SMOFA_visitor_image_saves\\" + svg_file_name_no_ext + ".svg");